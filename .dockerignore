# Скачанный корпус (сотни мегабайт) и кэш моделей в образ не нужны:
# корпус загружается отдельной командой, модель — в том при первом запуске.
data/
models/
.venv/
venv/

# Секреты — тем более
.env

__pycache__/
*.py[cod]
.pytest_cache/
.git/
.github/
reports/*.json
